<h2>トピック詳細</h2>
<div>
  <p>タイトル：<%= @topic.title %></p>
  <p>本文：<%= @topic.body %></p>
  <p>投稿日時：<%= @topic.created_at.strftime('%Y/%m/%d') %></p>
  <p>コメント件数：<%= @topic.comments.count %></p>
  <% @topic.comments.each do |comment| %>
  <p><%= comment.user.name %>
  <%= comment.created_at.strftime('%Y/%m/%d') %><%= comment.comment %>
  <% if comment.user == current_user %>
  <%= link_to "削除", topic_comment_path(comment.topic, comment), method: :delete %>
  <% end %></p>
<% end %>
</div>

<h2>コメント投稿</h2>
<div>
<%= form_with(model:[@topic, @comment], local: true) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに" %>
  <%= f.submit "送信する" %>
<% end %>
</div>